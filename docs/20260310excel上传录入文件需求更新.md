## excel上传需求结构

### 后端

mis/backend/app/
├── config                    # 完成excel表头和数据库的映射（**待完成**）
├── schemas/
│   ├── excel_schema.py       # 定义通用上传响应 + 各板块数据模型（**优先 project**）：
├── utils/
│   ├── excel_parser.py       # 通用Excel解析核心（所有板块复用）
│   ├── file_util.py          # 新增通用文件工具模块，核心目标是封装文件操作的通用逻辑
├── services/
│   ├── excel_service.py    # 多板块 Excel 处理服务Excel 数据的导入、导出、校验 核心逻辑
└── routers/
    ├── excel_router.py            # 项目导入接口

### 前端

